# ghcp-mcp-registry

Static GitHub-Pages-hosted MCP server registry for Cerebras, served from the `pages` branch at:

```
https://cerebras.github.io/ghcp-mcp-registry/registry/
```

See [`registry/README.md`](registry/README.md) for the spec, layout, and how to update the allowlist.
